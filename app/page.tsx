import React from 'react'

export default function page() {
  return (
    <div>
      {/* 
        react.js
        nó vừa hỗ trợ render dữ liệu ở phía client và phía server
        khi tạo component ở trong next.ls thì mặc định component đấy server component
        1. server component: mặc định khi các em tạo
        2. client component: phải thêm "use client" ở đầu
        cần tương tác dữ liệu dùng State, useEffect, các sưk kiện onClick
        Cách render dữ liệu ở trong Nextjs
        I-render dữ liệu ở phía server
        
      */}
    </div>
  )
}
